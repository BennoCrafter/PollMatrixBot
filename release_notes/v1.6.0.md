# Release Notes for Lunchy v1.6.0
- Added `!removeall` command: Use this command to remove all your added items from the current poll.
- Fixed case sensitivity for commands: All commands are now case-insensitive. `!Lunchy` now gets recognized as `!lunchy`.
